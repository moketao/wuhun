package
{
	import starling.display.DisplayObjectContainer;
	
	
	public class XStage extends XBox
	{
		public function XStage(p:DisplayObjectContainer,width:Number, height:Number, color:uint=0xffffff, borderColor:uint=0xff0000)
		{
			super(p,width,height,color,borderColor);
		}
	}
}